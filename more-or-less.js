const more = (n) => n + 1; // function that takes 1 argument and adds 1 to it
const less = (n) => n - 1; // function that takes 1 argument and less 1 from it

const add = (a,b) => (a + b); // function that takes 2 arguments and add them together
const sub = (a,b) => (a - b); // function that takes 2 arguments and sub the second from first 
